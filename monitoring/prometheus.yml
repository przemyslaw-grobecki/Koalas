global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "tempo"
    static_configs:
      - targets: ["tempo:3200"]

  # Scrape Prometheus metrics from our services via OTLP push
  # (services push metrics, Prometheus scrapes Tempo's metrics endpoint)